name: First Workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: macos-latest
    steps: 
      - name: Print greeting
        run: | 
            echo "Hello"
            echo "World"
            echo "!!!"
      - name: Print goodbye
        run: echo "Bye"
